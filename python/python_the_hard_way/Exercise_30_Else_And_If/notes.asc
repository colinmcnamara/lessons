== Lesson Notes

=== Ending w/ a colon:

Python always expects an indent after anything ended with a : so it knows what code to run.

=== forgeting to indent

Not indenting will normally result in an error. 

== Study Drills 

. Guess what elif and Else are doing 
* They are else if and else. if the if condition is not true, then elseif is processed. If neither is true, else is processed.
